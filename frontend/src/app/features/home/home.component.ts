import { Component } from '@angular/core';

/** El saludo y el menú viven en la sidebar (ver shared/nav-bar) — acá solo el contenido de inicio. */
@Component({
  selector: 'app-home',
  imports: [],
  templateUrl: './home.component.html',
})
export class HomeComponent {}
